# 与 pyproject.toml 保持一致，以 pyproject.toml 为唯一来源。
# 安装方式推荐: pip install -e .
# 若需此文件: pip install -r requirements.txt

# [project.dependencies]
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
numpy>=1.24.0
python-dotenv>=1.0.0
openai>=1.3.0
anthropic>=0.7.0
httpx>=0.25.0
sentence-transformers>=2.2.0

# [project.optional-dependencies] dev
# pip install -e ".[dev]" 安装开发依赖
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.6.0
